// import { createContext } from 'react';
// import { useState, useMemo, useEffect } from 'react';
// import PropTypes from 'prop-types';
// import { ContextMeals, MealRes } from '@/types';
// import { getAllMeals } from '@/app/lib/externalAPI';


// export const MealsContext = createContext<ContextMeals>({} as ContextMeals)

//  function  MealsProvider({ children }: { children: React.ReactNode }) {
//     const [allRecipes, setAllRecipes] = useState<MealRes[] | undefined>();

//     return (
//     );
// }

// MealsProvider.propTypes = {
//     children: PropTypes.node.isRequired,
// };

// export default MealsProvider;
